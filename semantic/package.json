{
  "name": "semantic",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "lint": "eslint . --ext ts,tsx",
    "build": "tsc && vite build",
    "test": "vitest --run",
    "test:watch": "vitest --silent=false"

  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "vite": "^5.2.0",
    "vitest": "^1.5.0"
  }
}
